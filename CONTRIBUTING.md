# 参与贡献

欢迎提交适合 C1 Slim 特性的应用、错误修复和文档补充。贡献前请遵守：

- 不提交原厂固件、launcher、个人设备备份、日志或密钥。
- 不提交没有明确再分发许可的 ROM、游戏、书籍、文章缓存或素材包。
- UI 必须适配 296×152 真正一位黑白显示，避免灰度、抖动、动画和定时刷新。
- 输入事件应被应用独占，并吞掉 Home（Terminal 的 Home 退出行为除外）。
- 安装器必须精确校验哈希、保留恢复文件并在失败时回滚。
- 新增应用至少提供操作说明、构建方法、设备路径和自检/预览方式。

提交前运行 `tools/preflight.ps1`，并尽量在真机确认退出后 launcher PID 不变、桌面帧恢复且刷新参数复原。
